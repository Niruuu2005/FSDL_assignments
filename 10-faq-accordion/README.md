Use these versions inside `10-faq-accordion`:

- `with-mongodb`: Node.js + Express + MongoDB version. Run `npm install` and `npm start` in that folder.
- `without-mongodb`: Static HTML/CSS/JS version. Open `index.html` directly.
